# PHPCS check temporarily disabled (PHP not in PATH)
# Uncomment when PHP is configured in your system PATH
# echo "🔍 Running PHPCS checks..."
# npm run lint:php:errors || {
#   echo "❌ PHPCS check failed! Please fix errors before committing."
#   exit 1
# }
# echo "✅ PHPCS checks passed!"

echo "🏗️  Building assets..."
npm run build || {
  echo "❌ Build failed! Please fix build errors before committing."
  exit 1
}

echo "✅ Build completed successfully!"
echo "✨ All checks passed! Ready to commit."
